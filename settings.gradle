pluginManagement {
    repositories {
        maven { url "https://arti.tw.ee/artifactory/libs-release" }
    }
}

include "tw-context"
include "tw-context-starter"
include "tw-context-ownership-starter"

rootProject.name = "tw-context-root"
