tw-context:
  ownership:
    entrypoint-to-owner-mappings:
      - "Jobs:testJob1:webapp-reliability"
      - "Web:/v1/profile/{profileId} (GET):profile-service"
    default-owner: "SRE" # lol, who else?